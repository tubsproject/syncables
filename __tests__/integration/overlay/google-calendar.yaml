openapi: 3.0.0
extends: https://raw.githubusercontent.com/tubsproject/syncables/refs/tags/v0.14.0/__tests__/integration/oad/google-calendar.yaml
info:
  description: Add syncables to the Google Calendar API.
actions:
- target: paths['/users/me/calendarList']['get']['responses']['200']['content']['application/json']
  update:
    syncables:
    - name: integrationTest
      description: |-
        Calendars of the currently authenticated user.
      type: collection
      paginationStrategy: pageToken
      itemsPathInResponse:
        - items
